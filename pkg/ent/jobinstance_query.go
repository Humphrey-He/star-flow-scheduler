// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/Humphrey-He/star-flow-scheduler/pkg/ent/jobinstance"
	"github.com/Humphrey-He/star-flow-scheduler/pkg/ent/predicate"
)

// JobInstanceQuery is the builder for querying JobInstance entities.
type JobInstanceQuery struct {
	config
	ctx        *QueryContext
	order      []jobinstance.OrderOption
	inters     []Interceptor
	predicates []predicate.JobInstance
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the JobInstanceQuery builder.
func (jiq *JobInstanceQuery) Where(ps ...predicate.JobInstance) *JobInstanceQuery {
	jiq.predicates = append(jiq.predicates, ps...)
	return jiq
}

// Limit the number of records to be returned by this query.
func (jiq *JobInstanceQuery) Limit(limit int) *JobInstanceQuery {
	jiq.ctx.Limit = &limit
	return jiq
}

// Offset to start from.
func (jiq *JobInstanceQuery) Offset(offset int) *JobInstanceQuery {
	jiq.ctx.Offset = &offset
	return jiq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (jiq *JobInstanceQuery) Unique(unique bool) *JobInstanceQuery {
	jiq.ctx.Unique = &unique
	return jiq
}

// Order specifies how the records should be ordered.
func (jiq *JobInstanceQuery) Order(o ...jobinstance.OrderOption) *JobInstanceQuery {
	jiq.order = append(jiq.order, o...)
	return jiq
}

// First returns the first JobInstance entity from the query.
// Returns a *NotFoundError when no JobInstance was found.
func (jiq *JobInstanceQuery) First(ctx context.Context) (*JobInstance, error) {
	nodes, err := jiq.Limit(1).All(setContextOp(ctx, jiq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{jobinstance.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (jiq *JobInstanceQuery) FirstX(ctx context.Context) *JobInstance {
	node, err := jiq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first JobInstance ID from the query.
// Returns a *NotFoundError when no JobInstance ID was found.
func (jiq *JobInstanceQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = jiq.Limit(1).IDs(setContextOp(ctx, jiq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{jobinstance.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (jiq *JobInstanceQuery) FirstIDX(ctx context.Context) int {
	id, err := jiq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single JobInstance entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one JobInstance entity is found.
// Returns a *NotFoundError when no JobInstance entities are found.
func (jiq *JobInstanceQuery) Only(ctx context.Context) (*JobInstance, error) {
	nodes, err := jiq.Limit(2).All(setContextOp(ctx, jiq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{jobinstance.Label}
	default:
		return nil, &NotSingularError{jobinstance.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (jiq *JobInstanceQuery) OnlyX(ctx context.Context) *JobInstance {
	node, err := jiq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only JobInstance ID in the query.
// Returns a *NotSingularError when more than one JobInstance ID is found.
// Returns a *NotFoundError when no entities are found.
func (jiq *JobInstanceQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = jiq.Limit(2).IDs(setContextOp(ctx, jiq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{jobinstance.Label}
	default:
		err = &NotSingularError{jobinstance.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (jiq *JobInstanceQuery) OnlyIDX(ctx context.Context) int {
	id, err := jiq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of JobInstances.
func (jiq *JobInstanceQuery) All(ctx context.Context) ([]*JobInstance, error) {
	ctx = setContextOp(ctx, jiq.ctx, "All")
	if err := jiq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*JobInstance, *JobInstanceQuery]()
	return withInterceptors[[]*JobInstance](ctx, jiq, qr, jiq.inters)
}

// AllX is like All, but panics if an error occurs.
func (jiq *JobInstanceQuery) AllX(ctx context.Context) []*JobInstance {
	nodes, err := jiq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of JobInstance IDs.
func (jiq *JobInstanceQuery) IDs(ctx context.Context) (ids []int, err error) {
	if jiq.ctx.Unique == nil && jiq.path != nil {
		jiq.Unique(true)
	}
	ctx = setContextOp(ctx, jiq.ctx, "IDs")
	if err = jiq.Select(jobinstance.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (jiq *JobInstanceQuery) IDsX(ctx context.Context) []int {
	ids, err := jiq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (jiq *JobInstanceQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, jiq.ctx, "Count")
	if err := jiq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, jiq, querierCount[*JobInstanceQuery](), jiq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (jiq *JobInstanceQuery) CountX(ctx context.Context) int {
	count, err := jiq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (jiq *JobInstanceQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, jiq.ctx, "Exist")
	switch _, err := jiq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (jiq *JobInstanceQuery) ExistX(ctx context.Context) bool {
	exist, err := jiq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the JobInstanceQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (jiq *JobInstanceQuery) Clone() *JobInstanceQuery {
	if jiq == nil {
		return nil
	}
	return &JobInstanceQuery{
		config:     jiq.config,
		ctx:        jiq.ctx.Clone(),
		order:      append([]jobinstance.OrderOption{}, jiq.order...),
		inters:     append([]Interceptor{}, jiq.inters...),
		predicates: append([]predicate.JobInstance{}, jiq.predicates...),
		// clone intermediate query.
		sql:  jiq.sql.Clone(),
		path: jiq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.JobInstance.Query().
//		GroupBy(jobinstance.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (jiq *JobInstanceQuery) GroupBy(field string, fields ...string) *JobInstanceGroupBy {
	jiq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &JobInstanceGroupBy{build: jiq}
	grbuild.flds = &jiq.ctx.Fields
	grbuild.label = jobinstance.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.JobInstance.Query().
//		Select(jobinstance.FieldCreatedAt).
//		Scan(ctx, &v)
func (jiq *JobInstanceQuery) Select(fields ...string) *JobInstanceSelect {
	jiq.ctx.Fields = append(jiq.ctx.Fields, fields...)
	sbuild := &JobInstanceSelect{JobInstanceQuery: jiq}
	sbuild.label = jobinstance.Label
	sbuild.flds, sbuild.scan = &jiq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a JobInstanceSelect configured with the given aggregations.
func (jiq *JobInstanceQuery) Aggregate(fns ...AggregateFunc) *JobInstanceSelect {
	return jiq.Select().Aggregate(fns...)
}

func (jiq *JobInstanceQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range jiq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, jiq); err != nil {
				return err
			}
		}
	}
	for _, f := range jiq.ctx.Fields {
		if !jobinstance.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if jiq.path != nil {
		prev, err := jiq.path(ctx)
		if err != nil {
			return err
		}
		jiq.sql = prev
	}
	return nil
}

func (jiq *JobInstanceQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*JobInstance, error) {
	var (
		nodes = []*JobInstance{}
		_spec = jiq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*JobInstance).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &JobInstance{config: jiq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, jiq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (jiq *JobInstanceQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := jiq.querySpec()
	_spec.Node.Columns = jiq.ctx.Fields
	if len(jiq.ctx.Fields) > 0 {
		_spec.Unique = jiq.ctx.Unique != nil && *jiq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, jiq.driver, _spec)
}

func (jiq *JobInstanceQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(jobinstance.Table, jobinstance.Columns, sqlgraph.NewFieldSpec(jobinstance.FieldID, field.TypeInt))
	_spec.From = jiq.sql
	if unique := jiq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if jiq.path != nil {
		_spec.Unique = true
	}
	if fields := jiq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, jobinstance.FieldID)
		for i := range fields {
			if fields[i] != jobinstance.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := jiq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := jiq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := jiq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := jiq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (jiq *JobInstanceQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(jiq.driver.Dialect())
	t1 := builder.Table(jobinstance.Table)
	columns := jiq.ctx.Fields
	if len(columns) == 0 {
		columns = jobinstance.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if jiq.sql != nil {
		selector = jiq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if jiq.ctx.Unique != nil && *jiq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range jiq.predicates {
		p(selector)
	}
	for _, p := range jiq.order {
		p(selector)
	}
	if offset := jiq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := jiq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// JobInstanceGroupBy is the group-by builder for JobInstance entities.
type JobInstanceGroupBy struct {
	selector
	build *JobInstanceQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (jigb *JobInstanceGroupBy) Aggregate(fns ...AggregateFunc) *JobInstanceGroupBy {
	jigb.fns = append(jigb.fns, fns...)
	return jigb
}

// Scan applies the selector query and scans the result into the given value.
func (jigb *JobInstanceGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, jigb.build.ctx, "GroupBy")
	if err := jigb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*JobInstanceQuery, *JobInstanceGroupBy](ctx, jigb.build, jigb, jigb.build.inters, v)
}

func (jigb *JobInstanceGroupBy) sqlScan(ctx context.Context, root *JobInstanceQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(jigb.fns))
	for _, fn := range jigb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*jigb.flds)+len(jigb.fns))
		for _, f := range *jigb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*jigb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := jigb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// JobInstanceSelect is the builder for selecting fields of JobInstance entities.
type JobInstanceSelect struct {
	*JobInstanceQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (jis *JobInstanceSelect) Aggregate(fns ...AggregateFunc) *JobInstanceSelect {
	jis.fns = append(jis.fns, fns...)
	return jis
}

// Scan applies the selector query and scans the result into the given value.
func (jis *JobInstanceSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, jis.ctx, "Select")
	if err := jis.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*JobInstanceQuery, *JobInstanceSelect](ctx, jis.JobInstanceQuery, jis, jis.inters, v)
}

func (jis *JobInstanceSelect) sqlScan(ctx context.Context, root *JobInstanceQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(jis.fns))
	for _, fn := range jis.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*jis.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := jis.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
