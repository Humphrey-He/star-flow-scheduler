// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/Humphrey-He/star-flow-scheduler/pkg/ent/executor"
)

// Executor is the model entity for the Executor schema.
type Executor struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// DeletedAt holds the value of the "deleted_at" field.
	DeletedAt *time.Time `json:"deleted_at,omitempty"`
	// ExecutorCode holds the value of the "executor_code" field.
	ExecutorCode string `json:"executor_code,omitempty"`
	// Host holds the value of the "host" field.
	Host string `json:"host,omitempty"`
	// IP holds the value of the "ip" field.
	IP string `json:"ip,omitempty"`
	// GrpcAddr holds the value of the "grpc_addr" field.
	GrpcAddr string `json:"grpc_addr,omitempty"`
	// HTTPAddr holds the value of the "http_addr" field.
	HTTPAddr *string `json:"http_addr,omitempty"`
	// Comma separated tags
	Tags *string `json:"tags,omitempty"`
	// Capacity holds the value of the "capacity" field.
	Capacity int `json:"capacity,omitempty"`
	// CurrentLoad holds the value of the "current_load" field.
	CurrentLoad int `json:"current_load,omitempty"`
	// Version holds the value of the "version" field.
	Version *string `json:"version,omitempty"`
	// Status holds the value of the "status" field.
	Status string `json:"status,omitempty"`
	// LastHeartbeatAt holds the value of the "last_heartbeat_at" field.
	LastHeartbeatAt time.Time `json:"last_heartbeat_at,omitempty"`
	// Metadata holds the value of the "metadata" field.
	Metadata     map[string]interface{} `json:"metadata,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Executor) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case executor.FieldMetadata:
			values[i] = new([]byte)
		case executor.FieldID, executor.FieldCapacity, executor.FieldCurrentLoad:
			values[i] = new(sql.NullInt64)
		case executor.FieldExecutorCode, executor.FieldHost, executor.FieldIP, executor.FieldGrpcAddr, executor.FieldHTTPAddr, executor.FieldTags, executor.FieldVersion, executor.FieldStatus:
			values[i] = new(sql.NullString)
		case executor.FieldCreatedAt, executor.FieldUpdatedAt, executor.FieldDeletedAt, executor.FieldLastHeartbeatAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Executor fields.
func (e *Executor) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case executor.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			e.ID = int(value.Int64)
		case executor.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				e.CreatedAt = value.Time
			}
		case executor.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				e.UpdatedAt = value.Time
			}
		case executor.FieldDeletedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field deleted_at", values[i])
			} else if value.Valid {
				e.DeletedAt = new(time.Time)
				*e.DeletedAt = value.Time
			}
		case executor.FieldExecutorCode:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field executor_code", values[i])
			} else if value.Valid {
				e.ExecutorCode = value.String
			}
		case executor.FieldHost:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field host", values[i])
			} else if value.Valid {
				e.Host = value.String
			}
		case executor.FieldIP:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field ip", values[i])
			} else if value.Valid {
				e.IP = value.String
			}
		case executor.FieldGrpcAddr:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field grpc_addr", values[i])
			} else if value.Valid {
				e.GrpcAddr = value.String
			}
		case executor.FieldHTTPAddr:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field http_addr", values[i])
			} else if value.Valid {
				e.HTTPAddr = new(string)
				*e.HTTPAddr = value.String
			}
		case executor.FieldTags:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field tags", values[i])
			} else if value.Valid {
				e.Tags = new(string)
				*e.Tags = value.String
			}
		case executor.FieldCapacity:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field capacity", values[i])
			} else if value.Valid {
				e.Capacity = int(value.Int64)
			}
		case executor.FieldCurrentLoad:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field current_load", values[i])
			} else if value.Valid {
				e.CurrentLoad = int(value.Int64)
			}
		case executor.FieldVersion:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field version", values[i])
			} else if value.Valid {
				e.Version = new(string)
				*e.Version = value.String
			}
		case executor.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				e.Status = value.String
			}
		case executor.FieldLastHeartbeatAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_heartbeat_at", values[i])
			} else if value.Valid {
				e.LastHeartbeatAt = value.Time
			}
		case executor.FieldMetadata:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field metadata", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &e.Metadata); err != nil {
					return fmt.Errorf("unmarshal field metadata: %w", err)
				}
			}
		default:
			e.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Executor.
// This includes values selected through modifiers, order, etc.
func (e *Executor) Value(name string) (ent.Value, error) {
	return e.selectValues.Get(name)
}

// Update returns a builder for updating this Executor.
// Note that you need to call Executor.Unwrap() before calling this method if this Executor
// was returned from a transaction, and the transaction was committed or rolled back.
func (e *Executor) Update() *ExecutorUpdateOne {
	return NewExecutorClient(e.config).UpdateOne(e)
}

// Unwrap unwraps the Executor entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (e *Executor) Unwrap() *Executor {
	_tx, ok := e.config.driver.(*txDriver)
	if !ok {
		panic("ent: Executor is not a transactional entity")
	}
	e.config.driver = _tx.drv
	return e
}

// String implements the fmt.Stringer.
func (e *Executor) String() string {
	var builder strings.Builder
	builder.WriteString("Executor(")
	builder.WriteString(fmt.Sprintf("id=%v, ", e.ID))
	builder.WriteString("created_at=")
	builder.WriteString(e.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(e.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	if v := e.DeletedAt; v != nil {
		builder.WriteString("deleted_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	builder.WriteString("executor_code=")
	builder.WriteString(e.ExecutorCode)
	builder.WriteString(", ")
	builder.WriteString("host=")
	builder.WriteString(e.Host)
	builder.WriteString(", ")
	builder.WriteString("ip=")
	builder.WriteString(e.IP)
	builder.WriteString(", ")
	builder.WriteString("grpc_addr=")
	builder.WriteString(e.GrpcAddr)
	builder.WriteString(", ")
	if v := e.HTTPAddr; v != nil {
		builder.WriteString("http_addr=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	if v := e.Tags; v != nil {
		builder.WriteString("tags=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	builder.WriteString("capacity=")
	builder.WriteString(fmt.Sprintf("%v", e.Capacity))
	builder.WriteString(", ")
	builder.WriteString("current_load=")
	builder.WriteString(fmt.Sprintf("%v", e.CurrentLoad))
	builder.WriteString(", ")
	if v := e.Version; v != nil {
		builder.WriteString("version=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(e.Status)
	builder.WriteString(", ")
	builder.WriteString("last_heartbeat_at=")
	builder.WriteString(e.LastHeartbeatAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("metadata=")
	builder.WriteString(fmt.Sprintf("%v", e.Metadata))
	builder.WriteByte(')')
	return builder.String()
}

// Executors is a parsable slice of Executor.
type Executors []*Executor
