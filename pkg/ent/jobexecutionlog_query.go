// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/Humphrey-He/star-flow-scheduler/pkg/ent/jobexecutionlog"
	"github.com/Humphrey-He/star-flow-scheduler/pkg/ent/predicate"
)

// JobExecutionLogQuery is the builder for querying JobExecutionLog entities.
type JobExecutionLogQuery struct {
	config
	ctx        *QueryContext
	order      []jobexecutionlog.OrderOption
	inters     []Interceptor
	predicates []predicate.JobExecutionLog
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the JobExecutionLogQuery builder.
func (jelq *JobExecutionLogQuery) Where(ps ...predicate.JobExecutionLog) *JobExecutionLogQuery {
	jelq.predicates = append(jelq.predicates, ps...)
	return jelq
}

// Limit the number of records to be returned by this query.
func (jelq *JobExecutionLogQuery) Limit(limit int) *JobExecutionLogQuery {
	jelq.ctx.Limit = &limit
	return jelq
}

// Offset to start from.
func (jelq *JobExecutionLogQuery) Offset(offset int) *JobExecutionLogQuery {
	jelq.ctx.Offset = &offset
	return jelq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (jelq *JobExecutionLogQuery) Unique(unique bool) *JobExecutionLogQuery {
	jelq.ctx.Unique = &unique
	return jelq
}

// Order specifies how the records should be ordered.
func (jelq *JobExecutionLogQuery) Order(o ...jobexecutionlog.OrderOption) *JobExecutionLogQuery {
	jelq.order = append(jelq.order, o...)
	return jelq
}

// First returns the first JobExecutionLog entity from the query.
// Returns a *NotFoundError when no JobExecutionLog was found.
func (jelq *JobExecutionLogQuery) First(ctx context.Context) (*JobExecutionLog, error) {
	nodes, err := jelq.Limit(1).All(setContextOp(ctx, jelq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{jobexecutionlog.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (jelq *JobExecutionLogQuery) FirstX(ctx context.Context) *JobExecutionLog {
	node, err := jelq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first JobExecutionLog ID from the query.
// Returns a *NotFoundError when no JobExecutionLog ID was found.
func (jelq *JobExecutionLogQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = jelq.Limit(1).IDs(setContextOp(ctx, jelq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{jobexecutionlog.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (jelq *JobExecutionLogQuery) FirstIDX(ctx context.Context) int {
	id, err := jelq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single JobExecutionLog entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one JobExecutionLog entity is found.
// Returns a *NotFoundError when no JobExecutionLog entities are found.
func (jelq *JobExecutionLogQuery) Only(ctx context.Context) (*JobExecutionLog, error) {
	nodes, err := jelq.Limit(2).All(setContextOp(ctx, jelq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{jobexecutionlog.Label}
	default:
		return nil, &NotSingularError{jobexecutionlog.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (jelq *JobExecutionLogQuery) OnlyX(ctx context.Context) *JobExecutionLog {
	node, err := jelq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only JobExecutionLog ID in the query.
// Returns a *NotSingularError when more than one JobExecutionLog ID is found.
// Returns a *NotFoundError when no entities are found.
func (jelq *JobExecutionLogQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = jelq.Limit(2).IDs(setContextOp(ctx, jelq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{jobexecutionlog.Label}
	default:
		err = &NotSingularError{jobexecutionlog.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (jelq *JobExecutionLogQuery) OnlyIDX(ctx context.Context) int {
	id, err := jelq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of JobExecutionLogs.
func (jelq *JobExecutionLogQuery) All(ctx context.Context) ([]*JobExecutionLog, error) {
	ctx = setContextOp(ctx, jelq.ctx, "All")
	if err := jelq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*JobExecutionLog, *JobExecutionLogQuery]()
	return withInterceptors[[]*JobExecutionLog](ctx, jelq, qr, jelq.inters)
}

// AllX is like All, but panics if an error occurs.
func (jelq *JobExecutionLogQuery) AllX(ctx context.Context) []*JobExecutionLog {
	nodes, err := jelq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of JobExecutionLog IDs.
func (jelq *JobExecutionLogQuery) IDs(ctx context.Context) (ids []int, err error) {
	if jelq.ctx.Unique == nil && jelq.path != nil {
		jelq.Unique(true)
	}
	ctx = setContextOp(ctx, jelq.ctx, "IDs")
	if err = jelq.Select(jobexecutionlog.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (jelq *JobExecutionLogQuery) IDsX(ctx context.Context) []int {
	ids, err := jelq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (jelq *JobExecutionLogQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, jelq.ctx, "Count")
	if err := jelq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, jelq, querierCount[*JobExecutionLogQuery](), jelq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (jelq *JobExecutionLogQuery) CountX(ctx context.Context) int {
	count, err := jelq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (jelq *JobExecutionLogQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, jelq.ctx, "Exist")
	switch _, err := jelq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (jelq *JobExecutionLogQuery) ExistX(ctx context.Context) bool {
	exist, err := jelq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the JobExecutionLogQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (jelq *JobExecutionLogQuery) Clone() *JobExecutionLogQuery {
	if jelq == nil {
		return nil
	}
	return &JobExecutionLogQuery{
		config:     jelq.config,
		ctx:        jelq.ctx.Clone(),
		order:      append([]jobexecutionlog.OrderOption{}, jelq.order...),
		inters:     append([]Interceptor{}, jelq.inters...),
		predicates: append([]predicate.JobExecutionLog{}, jelq.predicates...),
		// clone intermediate query.
		sql:  jelq.sql.Clone(),
		path: jelq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.JobExecutionLog.Query().
//		GroupBy(jobexecutionlog.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (jelq *JobExecutionLogQuery) GroupBy(field string, fields ...string) *JobExecutionLogGroupBy {
	jelq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &JobExecutionLogGroupBy{build: jelq}
	grbuild.flds = &jelq.ctx.Fields
	grbuild.label = jobexecutionlog.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.JobExecutionLog.Query().
//		Select(jobexecutionlog.FieldCreatedAt).
//		Scan(ctx, &v)
func (jelq *JobExecutionLogQuery) Select(fields ...string) *JobExecutionLogSelect {
	jelq.ctx.Fields = append(jelq.ctx.Fields, fields...)
	sbuild := &JobExecutionLogSelect{JobExecutionLogQuery: jelq}
	sbuild.label = jobexecutionlog.Label
	sbuild.flds, sbuild.scan = &jelq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a JobExecutionLogSelect configured with the given aggregations.
func (jelq *JobExecutionLogQuery) Aggregate(fns ...AggregateFunc) *JobExecutionLogSelect {
	return jelq.Select().Aggregate(fns...)
}

func (jelq *JobExecutionLogQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range jelq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, jelq); err != nil {
				return err
			}
		}
	}
	for _, f := range jelq.ctx.Fields {
		if !jobexecutionlog.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if jelq.path != nil {
		prev, err := jelq.path(ctx)
		if err != nil {
			return err
		}
		jelq.sql = prev
	}
	return nil
}

func (jelq *JobExecutionLogQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*JobExecutionLog, error) {
	var (
		nodes = []*JobExecutionLog{}
		_spec = jelq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*JobExecutionLog).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &JobExecutionLog{config: jelq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, jelq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (jelq *JobExecutionLogQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := jelq.querySpec()
	_spec.Node.Columns = jelq.ctx.Fields
	if len(jelq.ctx.Fields) > 0 {
		_spec.Unique = jelq.ctx.Unique != nil && *jelq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, jelq.driver, _spec)
}

func (jelq *JobExecutionLogQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(jobexecutionlog.Table, jobexecutionlog.Columns, sqlgraph.NewFieldSpec(jobexecutionlog.FieldID, field.TypeInt))
	_spec.From = jelq.sql
	if unique := jelq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if jelq.path != nil {
		_spec.Unique = true
	}
	if fields := jelq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, jobexecutionlog.FieldID)
		for i := range fields {
			if fields[i] != jobexecutionlog.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := jelq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := jelq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := jelq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := jelq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (jelq *JobExecutionLogQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(jelq.driver.Dialect())
	t1 := builder.Table(jobexecutionlog.Table)
	columns := jelq.ctx.Fields
	if len(columns) == 0 {
		columns = jobexecutionlog.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if jelq.sql != nil {
		selector = jelq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if jelq.ctx.Unique != nil && *jelq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range jelq.predicates {
		p(selector)
	}
	for _, p := range jelq.order {
		p(selector)
	}
	if offset := jelq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := jelq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// JobExecutionLogGroupBy is the group-by builder for JobExecutionLog entities.
type JobExecutionLogGroupBy struct {
	selector
	build *JobExecutionLogQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (jelgb *JobExecutionLogGroupBy) Aggregate(fns ...AggregateFunc) *JobExecutionLogGroupBy {
	jelgb.fns = append(jelgb.fns, fns...)
	return jelgb
}

// Scan applies the selector query and scans the result into the given value.
func (jelgb *JobExecutionLogGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, jelgb.build.ctx, "GroupBy")
	if err := jelgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*JobExecutionLogQuery, *JobExecutionLogGroupBy](ctx, jelgb.build, jelgb, jelgb.build.inters, v)
}

func (jelgb *JobExecutionLogGroupBy) sqlScan(ctx context.Context, root *JobExecutionLogQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(jelgb.fns))
	for _, fn := range jelgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*jelgb.flds)+len(jelgb.fns))
		for _, f := range *jelgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*jelgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := jelgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// JobExecutionLogSelect is the builder for selecting fields of JobExecutionLog entities.
type JobExecutionLogSelect struct {
	*JobExecutionLogQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (jels *JobExecutionLogSelect) Aggregate(fns ...AggregateFunc) *JobExecutionLogSelect {
	jels.fns = append(jels.fns, fns...)
	return jels
}

// Scan applies the selector query and scans the result into the given value.
func (jels *JobExecutionLogSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, jels.ctx, "Select")
	if err := jels.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*JobExecutionLogQuery, *JobExecutionLogSelect](ctx, jels.JobExecutionLogQuery, jels, jels.inters, v)
}

func (jels *JobExecutionLogSelect) sqlScan(ctx context.Context, root *JobExecutionLogQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(jels.fns))
	for _, fn := range jels.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*jels.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := jels.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
